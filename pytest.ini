[pytest]
# The tests import sheeter from the repo root.  Only "python -m pytest" puts the working
# directory on sys.path, so without this a bare "pytest" fails to collect anything, which
# is how CI runs it.
pythonpath = .
testpaths = tests
